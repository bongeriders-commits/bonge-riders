// ============================================
// NDUTHI FLIX — Pexels filler-content key
// ============================================
// Used by js/feed.js to pull free, commercially-licensed stock clips into
// the main feed whenever there aren't enough real rider veeds yet — so the
// app never looks empty to a new visitor, signed in or not.
//
// Get a free key at https://www.pexels.com/api/ (instant, no cost) and
// paste it below. This file ships inside the app bundle, so the key is
// visible to anyone who views page source — that's fine for Pexels (it's a
// public, rate-limited key, not a secret backend credential), but it does
// mean your quota (200 requests/hour, 20,000/month on the free tier) is
// shared by every visitor. If you ever see it being abused, regenerate the
// key from your Pexels account.
const PEXELS_API_KEY = "kNPo6aaQnOGzLC9tqfZDBUviXNcXnatbvU05S969DC2zctJc2YPTUr3X";
